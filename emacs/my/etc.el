(use-package spray :ensure t)
(use-package ereader :ensure t :defer t)
;; ereader has better org support. all book in one file, ^L separated; use ^X ]
;(use-package nov :ensure t
;  :config
;  (add-to-list 'auto-mode-alist '("\\.epub\\'" . nov-mode)))
